using UnityEngine;

public class USPropSound : MonoBehaviour {

	// Use this for initialization
	void Start () {
		AudioStarter.usprop = GetComponent<AudioSource>();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
