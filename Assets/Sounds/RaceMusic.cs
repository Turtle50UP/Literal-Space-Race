using UnityEngine;

public class RaceMusic : MonoBehaviour {

	// Use this for initialization
	void Start () {
		AudioStarter.race = GetComponent<AudioSource>();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
