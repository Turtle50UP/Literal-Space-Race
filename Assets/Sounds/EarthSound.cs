using UnityEngine;

public class EarthSound : MonoBehaviour {

	// Use this for initialization
	void Start () {
		AudioStarter.earth = GetComponent<AudioSource>();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
